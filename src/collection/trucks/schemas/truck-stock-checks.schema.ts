import { index, prop } from '@typegoose/typegoose'; import { BaseModel } from '../../../core/base.model';
export enum TruckStockCheckStatus { MATCHED='MATCHED', SHORTAGE='SHORTAGE', SURPLUS='SURPLUS', NOT_COUNTED='NOT_COUNTED', UNKNOWN='UNKNOWN', NOT_ON_TRUCK='NOT_ON_TRUCK', INVALID='INVALID' }
export class TruckStockCheckItem { @prop() productId?:string; @prop() productCode?:string; @prop() productName?:string; @prop() unit?:string; @prop({min:0}) systemQuantity?:number; @prop({min:0}) actualQuantity?:number; @prop() differenceQuantity?:number; @prop({enum:TruckStockCheckStatus,required:true}) status:TruckStockCheckStatus; @prop() note?:string; @prop() rowNumber?:number; }
@index({truckId:1,comparedAt:-1}) @index({syncIdempotencyKey:1},{unique:true,sparse:true}) export class TruckStockChecks extends BaseModel { @prop({required:true}) truckId:string; @prop({required:true}) truckCode:string; @prop({required:true}) truckName:string; @prop() truckLicensePlate?:string; @prop({required:true}) comparedAt:Date; @prop({type:()=>[TruckStockCheckItem],default:[]}) items:TruckStockCheckItem[]; @prop() summary:Record<string,number>; @prop() createdBy?:string; @prop() sourceFileName?:string; @prop() syncedAt?:Date; @prop() syncedBy?:string; @prop() syncReason?:string; @prop() syncIdempotencyKey?:string; @prop() backupId?:string; }
